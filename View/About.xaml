<UserControl x:Class="OnewheroVisitorManagement.View.About"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:OnewheroVisitorManagement.ViewModel">
    <UserControl.DataContext>
        <vm:AboutVM/>
    </UserControl.DataContext>
    
    <Grid>
        <Border CornerRadius="0,0,20,20"
                Background="White">
            <TextBlock Text="About Page"
                   Style="{StaticResource TitleText_Style}"/>
        </Border>
    </Grid>
</UserControl>
