<UserControl x:Class="OnewheroVisitorManagement.View.Services"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OnewheroVisitorManagement.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="850">
    <UserControl.DataContext>
        <vm:ServicesVM/>
    </UserControl.DataContext>
    
    <Grid>
        <Border CornerRadius="0,0,20,20" Background="White">
            <TextBlock Text="Services Page"
               Style="{StaticResource TitleText_Style}"/>
        </Border>
    </Grid>
</UserControl>
